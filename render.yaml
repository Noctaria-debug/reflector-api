services:
  - type: web
    name: reflector-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn reflector_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: REFLECTOR_API_KEY
        sync: false