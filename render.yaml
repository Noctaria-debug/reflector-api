services:
  - type: web
    name: reflector-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    uvicorn reflector_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: REFLECTOR_API_KEY
        sync: false